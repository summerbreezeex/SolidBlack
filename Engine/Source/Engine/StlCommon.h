#pragma once

#include <functional>
#include <list>
#include <map>
#include <memory>
#include <queue>
#include <set>
#include <string>
#include <sstream>
#include <vector>

#define foreach(var, container) \
    for (auto var = (container).begin(); \
         var != (container).end(); \
         ++var)


#pragma once

#include <functional>
#include <list>
#include <map>
#include <memory>
#include <queue>
#include <string>
#include <vector>

#define foreach(var, container) \
    for (auto var = (container).begin(); \
         var != (container).end(); \
         ++var)

